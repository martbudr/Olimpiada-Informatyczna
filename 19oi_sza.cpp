#include <bits/stdc++.h>

using namespace std;

const int N_MAX = 1e3, P_MAX = 1e6;
int n, p;

int main(void)
{
    ///ios_base::sync_with_stdio(0);
    ///cin.tie(0); cout.tie(0);

    cin >> n;
    for(int i=0; i<n; ++i){
        cin >>
    }
    cin >> p;
    for(int i=0; i<p; ++i){
        cin >>
    }
}
